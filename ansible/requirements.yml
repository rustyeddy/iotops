---
# Ansible Galaxy requirements file
# Install with: ansible-galaxy install -r requirements.yml

# Collections
collections:
  - name: community.general
    version: ">=7.0.0"
  
  - name: ansible.posix
    version: ">=1.5.0"

# Roles (uncomment and add as needed)
# roles:
#   - name: geerlingguy.docker
#     version: "7.0.0"
#
#   - name: geerlingguy.security
#     version: "3.0.0"
